# Travis CI Configuration file
# @link https://travis-ci.org/

# This is a sample file, to show how the scripts are meant to be used

# Using Java for the project
language: java
jdk:
# JDK 8 is set to deploy the docs in the configuration matrix
#  - oraclejdk8
# JDK 7 is set to deploy the artifacts in the configuration matrix
#  - oraclejdk7
  - openjdk7
matrix:
  include:
    # Code artifacts deployed with JDK7
    - jdk: oraclejdk7
      env: DEPLOY=true
    # Documentation deployed with JDK8
    - jdk: oraclejdk8
      env: DEPLOY_DOCS=true

before_install:
  # Gets scripts
  - git clone https://github.com/Bernardo-MG/ci-shell-scripts.git
  # Sets scripts as executable
  - chmod -R +x ci-shell-scripts/*
  # Prepares CI environment
  - source ci-shell-scripts/travis/load-travis-environment.sh
before_script:
  # Creates Maven settings
  - ci-shell-scripts/java/maven/create-maven-settings.sh $VERSION_TYPE
script:
  # Unit and integration tests are run
  - mvn clean verify
after_success:
  # Documentation deployment script
  - ci-shell-scripts/java/maven/deploy-site.sh $DO_DEPLOY_DOCS
  # Code artifacts deployment script
  - ci-shell-scripts/java/maven/deploy.sh $DO_DEPLOY
